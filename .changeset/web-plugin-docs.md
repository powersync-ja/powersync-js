---
'@powersync/web': patch
---

Documented the watched-query plugin API in the package README: registering plugins, per-query `extensions`, and the `source`/`sourceMeta` provenance fields (including what they mean for `useSuspenseQuery`).
