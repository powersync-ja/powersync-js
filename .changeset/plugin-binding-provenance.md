---
'@powersync/react': minor
'@powersync/vue': minor
---

Hook results now include `source` and `sourceMeta`, reporting whether the rows on screen are live or were seeded by a watched-query plugin (e.g. a cache), and hook options accept per-query plugin `extensions`.
