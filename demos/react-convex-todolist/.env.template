# Optional: copy this template to `.env.local` only when using custom service URLs.

# Self-hosted Convex Docker host ports
PS_CONVEX_PORT=3210
PS_CONVEX_SITE_PORT=3211

# Convex HTTP actions URL
VITE_CONVEX_SITE_URL=http://127.0.0.1:3211

# Convex backend URL
VITE_CONVEX_URL=http://127.0.0.1:3210

# PowerSync service URL
VITE_POWERSYNC_URL=http://localhost:8080
