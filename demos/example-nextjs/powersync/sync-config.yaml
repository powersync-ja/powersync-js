# yaml-language-server: $schema=https://unpkg.com/@powersync/service-sync-rules@latest/schema/sync_rules.json

config:
  edition: 3

streams:
  # Sync all todos to all users (anonymous / no auth filtering needed)
  global:
    auto_subscribe: true
    queries:
      - SELECT * FROM customers
